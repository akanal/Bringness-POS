(()=>{
  const $=id=>document.getElementById(id);
  const esc=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));
  const euro=c=>(Number(c||0)/100).toLocaleString("de-DE",{style:"currency",currency:"EUR"});
  const token=()=>localStorage.getItem("bringness-pos-token");
  async function api(path){const r=await fetch('/api/v1'+path,{headers:{authorization:'Bearer '+token()}});const j=await r.json();if(!r.ok)throw Error(j.error||'Fehler');return j}
  function download(name,type,text){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;document.body.appendChild(a);a.click();URL.revokeObjectURL(a.href);a.remove()}
  function csvCell(v){return '"'+String(v??'').replace(/"/g,'""')+'"'}
  function inRange(date){const from=$('taxFrom')?.value,to=$('taxTo')?.value,d=new Date(date),start=from?new Date(from+'T00:00:00'):null,end=to?new Date(to+'T23:59:59.999'):null;return (!start||d>=start)&&(!end||d<=end)}
  async function load(){const rid=$('restaurant')?.value;if(!rid)throw Error('Bitte zuerst einen Betrieb auswählen.');const [receipts,fiscal]=await Promise.all([api('/receipts?restaurantId='+encodeURIComponent(rid)),api('/fiscal/status?restaurantId='+encodeURIComponent(rid))]);return {rid,receipts:(receipts.receipts||[]).filter(r=>inRange(r.issued_at)),fiscal}}
  async function exportReceipts(){const d=await load();const rows=[['Belegnummer','Datum','Betrag EUR','Zahlungsart','Quelle','Fiskalstatus']];for(const r of d.receipts)rows.push([r.receipt_number,new Date(r.issued_at).toLocaleString('de-DE'),(Number(r.total_cents||0)/100).toFixed(2).replace('.',','),r.payment_method,r.source,r.fiscal_status]);download('Bringness-POS-Belegliste-'+new Date().toISOString().slice(0,10)+'.csv','text/csv;charset=utf-8','\ufeff'+rows.map(x=>x.map(csvCell).join(';')).join('\n'))}
  async function exportFiscal(){const d=await load();download('Bringness-POS-Fiskalstatus-'+new Date().toISOString().slice(0,10)+'.json','application/json;charset=utf-8',JSON.stringify({exportedAt:new Date().toISOString(),restaurantId:d.rid,filter:{from:$('taxFrom')?.value||null,to:$('taxTo')?.value||null},fiscalStatus:d.fiscal,receiptCount:d.receipts.length},null,2))}
  async function exportAuditPackage(){const d=await load(),total=d.receipts.reduce((s,r)=>s+Number(r.total_cents||0),0),pkg={format:'bringness-pos-audit-package-v1',officialDsfinvk:false,exportedAt:new Date().toISOString(),restaurantId:d.rid,period:{from:$('taxFrom')?.value||null,to:$('taxTo')?.value||null},summary:{receiptCount:d.receipts.length,totalCents:total,unsignedReceipts:Number(d.fiscal.unsignedReceipts||0)},fiscalStatus:d.fiscal,receipts:d.receipts};download('Bringness-POS-Pruefpaket-'+new Date().toISOString().slice(0,10)+'.json','application/json;charset=utf-8',JSON.stringify(pkg,null,2))}
  async function refresh(){const box=$('taxExportStatus');if(!box)return;box.textContent='Wird geladen…';try{const d=await load();const total=d.receipts.reduce((s,r)=>s+Number(r.total_cents||0),0),tse=d.fiscal.certified?'bereit':'nicht verbunden';box.innerHTML='<b>'+d.receipts.length+' Belege · '+esc(euro(total))+'</b><br>TSE: '+esc(tse)+' · Fiskalstatus: '+esc(d.fiscal.status||'unbekannt')+' · DSFinV-K: '+(d.fiscal.dsfinvkExportAvailable?'verfügbar':'noch nicht verfügbar')+'<br><small>'+esc(d.fiscal.message||'')+'</small>'}catch(e){box.textContent=e.message}}
  function mount(){if($('taxExportCenter'))return;const pages=[...document.querySelectorAll('.modulepage')];let target=pages.find(p=>/Belege|Rechnungen|Exporte/i.test(p.textContent||''))||pages.find(p=>p.id&&/beleg/i.test(p.id));if(!target)return setTimeout(mount,500);const sec=document.createElement('section');sec.id='taxExportCenter';sec.className='manage';sec.innerHTML='<h3>Steuerberater & Finanzamt – Exportzentrum</h3><p class="muted">Buchhaltungs- und Prüfexporte mit Zeitraumfilter. Das Bringness-Prüfpaket ist ein internes strukturiertes Exportformat und kein offizieller DSFinV-K-Export. DSFinV-K wird erst als verfügbar gekennzeichnet, wenn TSE und Exportformat vollständig eingerichtet und geprüft sind.</p><div style="display:flex;gap:9px;flex-wrap:wrap;align-items:end;margin-bottom:12px"><label>Von<br><input id="taxFrom" type="date"></label><label>Bis<br><input id="taxTo" type="date"></label><button type="button" id="taxThisMonth">Dieser Monat</button><button type="button" id="taxClearRange">Zeitraum löschen</button></div><div id="taxExportStatus" class="modulecard">Wird geladen…</div><div style="display:flex;gap:9px;flex-wrap:wrap;margin-top:12px"><button type="button" id="taxReceiptCsv">Belegliste als CSV</button><button type="button" id="taxFiscalJson">Fiskalstatus als JSON</button><button type="button" id="taxAuditJson">Prüfpaket als JSON</button><button type="button" id="taxRefresh">Status aktualisieren</button></div>';
    target.appendChild(sec);$('taxReceiptCsv').onclick=()=>exportReceipts().catch(e=>alert(e.message));$('taxFiscalJson').onclick=()=>exportFiscal().catch(e=>alert(e.message));$('taxAuditJson').onclick=()=>exportAuditPackage().catch(e=>alert(e.message));$('taxRefresh').onclick=refresh;$('taxThisMonth').onclick=()=>{const n=new Date(),f=new Date(n.getFullYear(),n.getMonth(),1),l=new Date(n.getFullYear(),n.getMonth()+1,0),fmt=x=>x.toISOString().slice(0,10);$('taxFrom').value=fmt(f);$('taxTo').value=fmt(l);refresh()};$('taxClearRange').onclick=()=>{$('taxFrom').value='';$('taxTo').value='';refresh()};$('taxFrom').onchange=refresh;$('taxTo').onchange=refresh;$('restaurant')?.addEventListener('change',refresh);refresh()}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',mount):mount();
})();
