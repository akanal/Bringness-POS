self.addEventListener("push",event=>{let d={};try{d=event.data?event.data.json():{}}catch(e){d={body:event.data?.text()||"Neue Bestellung"}}event.waitUntil(self.registration.showNotification(d.title||"Bringness POS",{body:d.body||"Neue Bestellung",tag:"bringness-waiter-order",renotify:true,vibrate:[300,120,300,120,500],data:{url:d.url||"/service/"}}))});self.addEventListener("notificationclick",event=>{event.notification.close();event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(ws=>{for(const w of ws){if("focus"in w){w.navigate(event.notification.data?.url||"/service/");return w.focus()}}return clients.openWindow(event.notification.data?.url||"/service/")}))});